# Cloudflare R2 Configuration for MySigMail
# R2 is S3-compatible, so we use the AWS SDK with R2 endpoints

# Your R2 Account ID (find in Cloudflare dashboard)
# Used to construct the endpoint URL
# Format: https://<account-id>.r2.cloudflarestorage.com
VITE_AWS_S3_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com

# Your R2 public bucket URL (for accessing uploaded images)
# Format: https://pub-<hash>.r2.dev OR your custom domain
VITE_AWS_S3_URL=https://pub-xxxxxxxxxxxxxxxxx.r2.dev

# Your R2 bucket name
VITE_AWS_S3_BASKET=mysigmail-images

# R2 Access Key ID (from R2 API tokens)
VITE_AWS_S3_ID=your_r2_access_key_id

# R2 Secret Access Key (from R2 API tokens)
VITE_AWS_S3_KEY=your_r2_secret_access_key

# R2 region - use 'auto' for Cloudflare R2
# R2 automatically routes to the nearest location
VITE_AWS_S3_REGION=auto

# Base path for the application (for path-based routing)
# Leave empty or set to '/' for root path
# Set to '/mysigmail' if deploying under a subpath
VITE_BASE_PATH=/
