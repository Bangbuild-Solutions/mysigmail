server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback - MUST come first to catch all routes
    location / {
        try_files $uri /index.html;
    }
}
